#include "main.h"
/**
 * _abs - is absolute function
 * Description - this function return the absolute value for its paramter
 * @n: is the parameter for return its absolute vlaue
 * Return: @n
 */

int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (0 - (n));
}
