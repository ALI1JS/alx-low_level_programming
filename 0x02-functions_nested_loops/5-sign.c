#include "main.h"

/**
 * print_sign - is a function sign checker
 * @n: is the parameter for checker
 * Return:1 or 0 or -1
 */

int print_sign(int n)
{
if (n > 0)
{
return (1);
}
else if (n == 0)
{
return (0);
}
return (-1);
}
